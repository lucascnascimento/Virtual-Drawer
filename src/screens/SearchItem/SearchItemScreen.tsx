import React from 'react';
import { Text, View } from 'react-native';

import { SearchItemScreenProps } from '~/types/types';

// import { Container } from './styles';

const SearchItemScreen: React.FC<SearchItemScreenProps> = () => {
  return <Text>SearchItemScreen</Text>;
};

export default SearchItemScreen;
